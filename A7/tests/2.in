POST signup ? username passenger1 role passenger
POST signup ? username passenger2 role passenger
POST signup ? username passenger2 role passenger
POST signup ? username passenger3 role passenger
POST signup ? username passenger4 role passenger
POST signup ? role driver username passenger1
POST signup ? role driver username deriver1
POST signup ? role driver username deriver2
POST signup ? role driver username deriver3
POST trips ? username passenger1 destination Tehranpars origin Enghelab
POST trips ? username passenger2 destination Enghelab origin Marzdaran
POST trips ? username passenger3 destination Enghelab origin Tehranpars
POST trips ? username passenger4 destination Marzdaran origin Enghelab
DELETE trips ? username passenger1 id 5
DELETE trips ? username passenger1 id 1
POST accept ? username deriver1 id 2
POST accept ? username deriver1 id 3
POST accept ? username deriver2 id 4
GET trips ? username deriver1
POST finish ? username deriver1 id 2
POST finish ? username deriver2 id 4
GET trips ? username deriver2